# Relaybase TUI Binaries

Generated `relaybase-tui` binaries are written here by:

```powershell
npm run tui:build
npm run tui:build:all
```

The Node CLI bridge looks here after `RELAYBASE_TUI_BIN` and before optional platform-specific packages.
