interface:
  display_name: "Relaybase Dev"
  short_description: "Develop apps through Relaybase first"
  default_prompt: "Use $relaybase-dev to run this app through Relaybase with direct ports only as fallback."
